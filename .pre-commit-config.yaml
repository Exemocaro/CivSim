repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.0
    hooks:
      - id: ruff          # linter
        args: [--fix]
      - id: ruff-format   # formatter (replaces black)
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.10.0
    hooks:
      - id: mypy
        additional_dependencies: [pygame-stubs]
        args: [--ignore-missing-imports]
